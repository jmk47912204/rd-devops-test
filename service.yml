apiVersion: v1
kind: Service 
metadata:
  name: rendertron-svc
spec:
  type: LoadBalancer # ClusterIP, # NodePort, # LoadBalancer
  selector:
    app: rendertron
  ports: 
    - name: http
      port: 80 # Service Port
      targetPort: 3000 # Container Port
