# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: rendertron-ingress-service
#   annotations:
#     kubernetes.io/ingress.class: nginx  
# spec:  
#   rules:
#   - http:
#       paths:
#       - path: /
#         backend:
#           serviceName: rendertron-clusterip-service
#           servicePort: 80
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rendertron-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /testpath
        pathType: Prefix
        backend:
          service:
            name: test
            port:
              number: 80
